Algoritmo "tabuada"

Var

v, i, total : inteiro

Inicio

      escreva("Deseja a tabuada para qual valor? ")
      leia(v)
      
      para i de 1 ate 10 faca
         total <- v * i
         escreval(v, " X ", i, " = ", total)
      fimpara
      


Fimalgoritmo